kotlinter {
    ignoreFailures = false
    indentSize = 4
    continuationIndentSize = 4
    reporters = [ "checkstyle", "plain" ]
    experimentalRules = false
    disabledRules = [ "no-blank-line-before-rbrace", "import-ordering" ]
}
